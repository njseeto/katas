// Use the array index to identify which orders were first.

// Function --> input 3x arrays, output boolean

const serviceChecker = ( takeOutOrders, dineInOrders, servedOrders ) => {
    
    // use indexOf() method
    // clone the arrays, replace the elements in the takeOut and dineIn arrays with indices.
    // map the number to the index
    // check the servedOrder array for the corresponding index and check if the left number is less than or equal to the right
    
}